# Local development environment variables (copy to .env.development.local)
# For testing on mobile devices or other machines on the same network
# Replace 192.168.1.100 with your actual machine's IP address

# To find your IP address:
# - Linux/Mac: Run `ip addr show` or `ifconfig`
# - Windows: Run `ipconfig`
# Look for your local network IP (usually starts with 192.168.x.x or 10.x.x.x)

VITE_API_URL=http://192.168.1.100:4000

# Instructions:
# 1. Copy this file to .env.development.local
# 2. Replace the IP address with your machine's actual IP
# 3. Restart the dev server (npm run dev)
# 4. Access the app from your mobile using: http://YOUR_MACHINE_IP:5173
